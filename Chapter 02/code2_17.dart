main() {
  List<int> integers = [1, 2, 3, 4, 5];
// Adding elements to the list
  integers.add(6);
// Deleting elements from the list
  integers.remove(5);
// Sorting a list
  integers.sort();
// Clear the list
  integers.clear();
}
