enum Color {
  red,
  blue,
  yellow,
}

main() {
  Color color = Color.red;

  switch (color) {
    case Color.red:
      break;
    case Color.blue:
      break;
    case Color.yellow:
      break;
  }
}
